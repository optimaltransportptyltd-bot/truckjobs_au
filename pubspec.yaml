name: truckjobs_au
description: "TruckJobs AU app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  firebase_core: ^4.1.0
  cloud_firestore: ^6.0.0
  firebase_auth: ^6.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/truck_banner.png
    - assets/images/truckbanner.jpg
    - assets/images/truckbanner2.jpg